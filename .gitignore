# compiled output
/dist
/node_modules
/payment-lambda-a/node_modules

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Terraform state files
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Lock files (used to pin provider versions)
.terraform.lock.hcl
terraform.lock.hcl

# Terraform cache and local directories
.terraform/

# Terraform plan files (used to store plan execution results)
*.tfplan

# Sensitive variable files (potentially containing credentials or secrets)
*.tfvars
*.tfvars.json


# Ignorar todos los archivos .js generados
*.js

# Ignorar todos los archivos .zip generados
*.zip

# Ignorar la carpeta dist/ o build/ si se generan archivos compilados aquí (opcional)
dist/
build/

# Si hay excepciones y deseas rastrear archivos .js específicos, puedes usar esta línea:
# !src/keep-me.js
